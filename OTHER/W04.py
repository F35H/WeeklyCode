import time
import queue  as q

#Shuffling Algorithm


def MDMethod():
  tStr = str( time.time() )
  shufSeed = ""

  num = int(tStr[-1]
    +tStr[-2]+tStr[-3]+tStr[-4] )
  already_seen = set()
  counter = 0

  for i in range(20):
    counter += 1
    already_seen.add(number)
    number = int(str(number * number).zfill(8)[2:6])  # zfill adds padding of zeroes
    
    shufSeed.join(str(number))
    if (counter == 37): counter = 0

  return dckSeed

shufSeed = int( seed )
dckSize = int(input( "Enter Deck Size:" ))
shufDck = q.LifoQueue(dckSize)
newDeck = list( range(dckSize) )

def PokShuf():
  for in in range(dckSize):
  
  #Split Deck in Half
  #Determine packets
    newDeck[]


